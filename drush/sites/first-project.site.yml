local:
    root: /var/www/first-project/docroot
    uri: 'http://local.first-project.com'
    host: local.first-project.com
    user: vagrant
    ssh: { options: '-o StrictHostKeyChecking=no -o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
